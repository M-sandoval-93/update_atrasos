// let lista = document.querySelectorAll('.barra_navegacion li');

// function activeLink() {
//     lista.forEach((item) => item.classList.remove('hover_ed'));
//     this.classList.add('hover_ed');  
// }

// lista.forEach((item) => item.addEventListener('mouseover', activeLink));